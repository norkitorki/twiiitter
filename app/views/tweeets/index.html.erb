<section class="section">
  <div class="container">
    <div class="columns">
      <%= render 'trends' %>
      <%= render 'feed', tweeets: @tweeets %>
      <%= render 'who-to-follow' %>
    </div>
  </div>
</section>
